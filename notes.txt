npm install -g -expo-cli

npx create-expo-app [appName]

cd [appName]

npm start 

npx expo start -c 
npm start --reset-cache

npm install eslint --save-dev

npx eslint --init

npm install eslint globals @eslint/js eslint-plugin-react --legacy-peer-deps

npm install @react-native-community/eslint-config --save-dev

npm i --save-dev @types/eslint-plugin-react

npm install @react-navigation/native --legacy-peer-deps

npx expo install react-native-screens react-native-safe-area-context --legacy-peer-deps


EXPO, ESLINT, NativeWind, SafeAreaView


 git init -b [repo name]

 git commit -m "First commit"

 git remote add origin https://github.com/DDstar1/weather_app.git

 git push -u origin main

git push origin HEAD:main